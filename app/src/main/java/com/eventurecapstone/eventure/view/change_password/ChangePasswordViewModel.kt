package com.eventurecapstone.eventure.view.change_password

import androidx.lifecycle.ViewModel
import com.eventurecapstone.eventure.data.repository.PreferenceRepository

class ChangePasswordViewModel(private val preferenceRepository: PreferenceRepository): ViewModel() {
}